target: 'agnes'

config:
  kind: EXECUTABLE

dependencies:
  private:
    - libclang
    - reflection
    - kainjow_mustache
    - nlohmann_json

source:
  paths: '.'
  patterns:
    - '*.cpp'

defines:
    private:
    - 'ANNOTATION_GENERATOR'
